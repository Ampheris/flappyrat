
export const GRAVITY = 0.15;
export const JUMP_STRENGTH = -6;
export const PIPE_SPEED = 1.8;
export const PIPE_WIDTH = 60; 
export const PIPE_CAP_WIDTH = 68;
export const PIPE_CAP_HEIGHT = 30;
export const MIN_GAP = 160; // px
export const MAX_GAP = 240; // px
export const PIPE_SPAWN_RATE = 200; // frames
export const RAT_SIZE = 44; // px
export const RAT_HITBOX_SIZE = 28; // px
export const RAT_X_OFFSET = 60; // px
export const MIN_PIPE_HEIGHT = 80; // Minimum height of a pipe segment (must be > cap height)
